{
  "name": "license_server",
  "version": "1.0.0",
  "description": "This is the server that built by Express and MongoDB.",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && cp -r ../angular/dist dist",
    "start": "rimraf dist && cp -r ../angular/dist dist && node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Zhangxu",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.17.2",
    "connect-flash": "^0.1.1",
    "connect-mongo": "^2.0.0",
    "cookie-parser": "^1.4.3",
    "express": "^4.15.3",
    "express-session": "^1.15.6",
    "mongodb": "^2.2.29",
    "mongoose": "^4.12.2",
    "passport": "^0.4.0",
    "passport-local": "^1.0.0"
  },
  "devDependencies": {
    "rimraf": "^2.6.2"
  }
}
